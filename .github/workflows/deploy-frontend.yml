name: Deploy Frontend to Azure Static Web Apps

on:
  push:
    branches: [ main ]
  workflow_dispatch:

# Prevent overlapping deployments
concurrency:
  group: frontend-deploy-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    name: Deploy to Azure Static Web Apps
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v5

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20.x'
        cache: 'npm'

    - name: Environment Variable Validation
      run: |
        echo "🔍 Validating required environment variables..."
        
        # Check AZURE_STATIC_WEB_APPS_API_TOKEN
        if [ -z "${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}" ]; then
          echo "❌ AZURE_STATIC_WEB_APPS_API_TOKEN is required"
          echo "   Get this from: Azure Portal → Static Web Apps → Deployment token"
          exit 1
        else
          echo "✅ AZURE_STATIC_WEB_APPS_API_TOKEN: ${AZURE_STATIC_WEB_APPS_API_TOKEN:0:4}***"
        fi
        
        # Check PRODUCTION_API_URL
        if [ -z "${{ secrets.PRODUCTION_API_URL }}" ]; then
          echo "❌ PRODUCTION_API_URL is required"
          echo "   This should be your backend API URL (e.g., https://your-backend.azurewebsites.net)"
          exit 1
        else
          echo "✅ PRODUCTION_API_URL: ${{ secrets.PRODUCTION_API_URL }}"
        fi
        
        echo "✅ Environment validation passed"

    - name: Install client dependencies
      working-directory: ./client
      run: npm ci

    - name: Build client
      working-directory: ./client
      run: npm run build
      env:
        VITE_API_URL: ${{ secrets.PRODUCTION_API_URL }}

    - name: Deploy to Azure Static Web Apps
      uses: Azure/static-web-apps-deploy@v1
      with:
        action: "upload"
        azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
        repo_token: ${{ secrets.GITHUB_TOKEN }}
        app_location: "./client"
        output_location: "dist"
        app_build_command: "npm ci && npm run build"
        deployment_environment: production

    - name: Get deployment URL
      id: deployment_url
      run: |
        echo "url=https://${{ secrets.AZURE_STATIC_WEB_APP_NAME }}.azurestaticapps.net" >> $GITHUB_OUTPUT

    - name: Wait for deployment
      run: sleep 30

    - name: Run smoke tests
      run: |
        echo "🔍 Running post-deployment smoke tests..."
        
        # Test frontend deployment
        curl -sSf ${{ steps.deployment_url.outputs.url }} || {
          echo "❌ Frontend deployment test failed"
          exit 1
        }
        
        echo "✅ Frontend deployment test passed"
        echo "✅ All smoke tests passed"

    - name: Notify deployment success
      if: success()
      run: |
        echo "🎉 Frontend successfully deployed to Azure Static Web Apps!"
        echo "Production URL: ${{ steps.deployment_url.outputs.url }}"
        echo "Backend API: ${{ secrets.PRODUCTION_API_URL }}"

    - name: Notify deployment failure
      if: failure()
      run: |
        echo "❌ Frontend deployment failed!"
        echo "Check the logs above for details."
        echo "Ensure AZURE_STATIC_WEB_APPS_API_TOKEN and PRODUCTION_API_URL are set in GitHub Secrets"
